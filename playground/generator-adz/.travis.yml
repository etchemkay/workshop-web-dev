language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-adz' ]; then cd .. && eval "mv $currentfolder generator-adz" && cd generator-adz; fi

